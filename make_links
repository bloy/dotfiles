#!/bin/bash

FILELIST=(.bash_complete .bash_logout .bash_profile .bashrc bin/keychain-ssh \
  bin/keychain-wrapper .cvsrc .dircolors .ssh/config .vimrc .vim .Xresources)
PWD=`pwd`
if [ ! -d ~/bin ]; then
  mkdir ~/bin
fi
for i in ${FILELIST[*]}; do
  echo $i
  rm ~/$i
  ln -s ${PWD}/$i ~/$i
done

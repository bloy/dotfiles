# vim: ft=sh
tmux new-window -t $SESSION -d
tmux send-keys -t ${SESSION}:1.0 'vim' C-m

if [ -e "$DIR/Guardfile" -a -e "$DIR/Gemfile" ]; then
  tmux new-window -t $SESSION -d
  tmux send-keys -t $SESSION:3.0 'bundle exec guard' C-m
fi

tmux select-window -t ${SESSION}:1

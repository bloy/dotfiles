# vim: ft=sh
#tmux set-environment -t $SESSION DJANGO_SETTINGS_MODULE "$SESSION.settings"

. "$SESSIONCFG/python"

tmux new-window -t $SESSION -n procs -d
tmux send-keys -t ${SESSION}:3.0 './manage.py runserver' C-m
tmux select-pane -t ${SESSION}:3.0
